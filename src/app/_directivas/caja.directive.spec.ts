import { CajaDirective } from './caja.directive';

describe('CajaDirective', () => {
  it('should create an instance', () => {
    const directive = new CajaDirective();
    expect(directive).toBeTruthy();
  });
});
